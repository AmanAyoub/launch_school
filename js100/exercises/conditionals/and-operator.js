//  (&&) and logical operator takes two operands
// and returns true only if both operands are true otherwise returns false:

if (true && false) {
  console.log('Yes!');
} else {
  console.log('No...');
}
// returns No...

// On line 4, inside the if statement condition we used (&&) operator, since one of the operands is false
// it returns false, so the else block will be executed.


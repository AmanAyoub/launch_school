let randomNumber = Math.round(Math.random());


if (randomNumber === 1) {
	// If randomNumber equals 1, this block of code will execute.
	console.log('Yes!');
} else {
	// If randomNumber doesn't equals to 1, this block will be executed.
	console.log('No.');
}


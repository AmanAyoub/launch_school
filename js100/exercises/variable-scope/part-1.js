// What will the following code log to the console and why?

console.log(greeting); // returns undefined

var greeting = 'Hello world!';

// It returns a ReferenceError, since we cannot access a variable before declaring it.

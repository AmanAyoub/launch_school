// What will the following code log to the console and why?

// It returns undefined
console.log(greeting);

let greeting = 'Hello world!'; // ReferenceError: Cannot access 'greeting' before initialization

// It returns ReferenceError since we cannot access variables declared with let keyword before declaring it.

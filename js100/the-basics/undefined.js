// The undefined property indicates that a variable has not been assigned a value, or not declared at all.
// We can also explicitly use the literal undefined.

let fruit;
console.log(fruit); // returns undefined

let grade = undefined;
console.log(grade); // returns undefined

let team = ['Ajmal', 'Ahmad', undefined, 'Aman'];
console.log(team[2]); // returns undefined
